/** Set by AuthOwl on a response that established a browser session. */
export const SESSION_TOKEN_HEADER = 'set-auth-token';

/** Paired declaration required before a bearer credential is treated as a session. */
export const SESSION_TRANSPORT_HEADER = 'x-authowl-session-transport';
export const SESSION_TRANSPORT_BEARER = 'bearer';
