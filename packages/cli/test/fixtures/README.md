# Detector fixture corpus

These trees cover supported layouts and ambiguous/deferred layouts. Detector
tests hash every file before and after classification to guarantee this phase is
strictly read-only.
